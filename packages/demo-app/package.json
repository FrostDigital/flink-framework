{
  "name": "@flink/demo-app",
  "version": "0.2.0-alpha.10",
  "description": "Demo app used as documentation and to run integration tests",
  "private": true,
  "scripts": {
    "test": "node --preserve-symlinks -r jasmine-ts",
    "start": "node --preserve-symlinks -r ts-node/register -- src/index.ts --project tsconfig.json",
    "dev": "npm run flink:generate && nodemon",
    "prestart": "npm run flink:generate",
    "pretest": "npm run flink:generate",
    "flink:generate": "flink generate"
  },
  "dependencies": {
    "@flink-app/api-docs-plugin": "^0.2.0-alpha.10",
    "@flink-app/firebase-messaging-plugin": "^0.2.0-alpha.10",
    "@flink-app/flink": "^0.2.0-alpha.10",
    "@flink-app/jwt-auth-plugin": "^0.2.0-alpha.10",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@types/jasmine": "^3.7.1",
    "@types/node": "^15.0.1",
    "@types/uuid": "^8.3.0",
    "got": "^9.6.0",
    "jasmine": "^3.7.0",
    "jasmine-spec-reporter": "^7.0.0",
    "jasmine-ts": "^0.3.3",
    "nodemon": "^2.0.7",
    "ts-node": "^10.0.0",
    "typescript": "4.2.3"
  }
}
