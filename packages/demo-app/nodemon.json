{
  "watch": ["src"],
  "ext": "ts,json",
  "ignore": ["*.spec.ts"],
  "exec": "$(npm bin)/flink generate && node --preserve-symlinks -r ts-node/register -- src/index.ts",
  "watchOptions": {
    "followSymlinks": false
  }
}
