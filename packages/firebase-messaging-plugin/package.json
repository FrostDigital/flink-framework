{
  "name": "@flink-app/firebase-messaging-plugin",
  "version": "0.2.0-alpha.0",
  "description": "Flink plugin to send Firebase cloud messages",
  "scripts": {
    "test": "echo \"Error: no test specified\"",
    "prepublish": "npm run flink:generate && tsc",
    "watch": "npm run flink:generate && tsc-watch",
    "prestart": "npm run flink:generate",
    "pretest": "npm run flink:generate",
    "flink:generate": "flink generate-schema"
  },
  "author": "joel@frost.se",
  "license": "MIT",
  "types": "dist/index.d.ts",
  "main": "dist/index.js",
  "dependencies": {
    "fcm-push": "^1.1.3"
  },
  "devDependencies": {
    "@flink-app/flink": "^0.2.0-alpha.0",
    "@types/express": "^4.17.12",
    "@types/node": "^15.6.2",
    "jasmine-spec-reporter": "^7.0.0",
    "nodemon": "^2.0.7",
    "ts-node": "^9.1.1",
    "tsc-watch": "^4.2.9",
    "typescript": "^4.2.4"
  }
}
